/** Public run state. Lease credentials are never included in browser records. */
export type AgentRun={id:string;companyId:string;channel:string;conversationId:string;parentId:string|null;prompt:string;requestedBy:string;requesterName:string;agentId:string;agentName:string;status:'queued'|'running'|'succeeded'|'failed'|'cancelled';attempts:number;maxAttempts:number;availableAt:string;leaseExpiresAt:string|null;createdAt:string;updatedAt:string;startedAt:string|null;deadlineAt:string|null;finishedAt:string|null;result:string;error:string;artifactUrl:string|null;resultMessageId:string|null;capabilities:string[]};
